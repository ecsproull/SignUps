# Keep line endings consistent for *your* source files.
# We intentionally do NOT enforce line endings inside vendor/ to avoid huge diffs.

# Default behavior: let git detect text files.
* text=auto

# Enforce LF for project source.
*.php text eol=lf
*.js text eol=lf
*.css text eol=lf
*.scss text eol=lf
*.json text eol=lf
*.xml text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Windows scripts should stay CRLF when present.
*.bat text eol=crlf
*.cmd text eol=crlf

# Do not normalize vendor files.
vendor/** -text
