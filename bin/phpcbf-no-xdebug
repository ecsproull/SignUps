#!/usr/bin/env bash
set -euo pipefail

# Run PHPCBF using a known PHP binary (avoid relying on `php` in PATH).
export XDEBUG_MODE=off
exec /usr/bin/php /var/www/wordpress/wp-content/plugins/SignUps/vendor/bin/phpcbf "$@"
