#!/usr/bin/env bash
set -euo pipefail

export XDEBUG_MODE=off

PHP_BIN="${PHP_BIN:-}"
if [[ -z "$PHP_BIN" ]]; then
  if command -v php >/dev/null 2>&1; then
    PHP_BIN="$(command -v php)"
  else
    PHP_BIN="/usr/bin/php"
  fi
fi

exec "$PHP_BIN" "$@"
